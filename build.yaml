apiVersion: v1
kind: Pod
metadata:
  labels:
    some-label: pod
spec:
  containers:
    - name: golang
      image: golang:alpine
      command:
        - cat
      tty: true
      # volumeMounts:
      #   - name: m2
      #     mountPath: /root/.m2
    - name: docker
      image: docker:latest
      command:
        - cat
      tty: true
      privileged: true
      # volumeMounts:
      #   - name: dockersock
      #     mountPath: /var/run/docker.sock
    - name: helm
      image: dtzar/helm-kubectl
      command:
        - cat
      tty: true
  # volumes:
  #   - name: dockersock
  #     hostPath:
  #       path: /var/run/docker.sock
  #   - name: m2
  #     hostPath:
  #       path: /root/.m2